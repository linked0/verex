{
  "name": "@verex/sdk",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "sync-abis": "node scripts/sync-abis.mjs",
    "prebuild": "pnpm sync-abis",
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "clean": "rm -rf dist src/abis"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "viem": "^2.21.0"
  }
}

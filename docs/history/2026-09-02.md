### Feature index: restore category table rendering

Added the required blank line between the Categories heading and its Markdown table so the documentation renderer recognizes the rows as a table instead of displaying the pipe syntax as paragraph text.

### Verex checkout: synchronize with work main

Fast-forwarded the work-agent checkout from `cbc8a1f` to the work checkout's `934be01`, preserving the local documentation-table fix. The two Verex checkouts now share the same mainline commit.
